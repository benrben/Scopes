---
description: Implement changes via strict TDD (red/green/refactor)
---

# Develop (TDD)

Implement features and fixes using strict Test-Driven Development: write failing test first, minimal code, refactor, update Scopes.

**Read and follow the skill instructions in** `skills/developing-tdd/SKILL.md`.

If `$ARGUMENTS` is provided, use it as the task to implement: "$ARGUMENTS"
