---
description: Implement changes with sandbox verification (no test writing)
---

# Develop (Verified)

Implement features and fixes via sandbox terminal verification — run existing tests, scripts, REPL, curl. Does NOT write new test files.

**Read and follow the skill instructions in** `skills/developing-verified/SKILL.md`.

If `$ARGUMENTS` is provided, use it as the task to implement: "$ARGUMENTS"
