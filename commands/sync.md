---
description: Generate or update Scopes documentation from code reality
---

# Sync Scopes

Generate or update the `Scopes/` documentation layer from observable code, tests, config, and schema.

**Read and follow the skill instructions in** `skills/syncing-scopes/SKILL.md`.

If `$ARGUMENTS` is provided, use it as context for which areas or capabilities to focus on: "$ARGUMENTS"

If no arguments, run a full sync (generation or update depending on whether `Scopes/` exists).
