# Changelog

## Unreleased

- Add `name` frontmatter to all `commands/*.md` for explicit registration.
- Add `repository` and `icon` fields to the Claude Code plugin manifest.
- Add docs + templates for `.claude/settings.local.json` and `.claude/settings.local.md`.
- Make bundled helper-script paths resilient via `$CLAUDE_PLUGIN_ROOT` / `SKILLS_ROOT`.
- Remove skill/agent modules for a smaller minimal profile.
- Remove `.DS_Store` and lint against reintroducing it.
- Add MIT `LICENSE`.
